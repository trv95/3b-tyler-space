{
  "post": {
    "summary": "Submit an access request",
    "description": "Normalizes the form submission and forwards it to Process request, which creates a Tines case and posts an interactive Slack approval message. Returns the created case.",
    "requestBody": {
      "required": true,
      "content": {
        "application/json": {
          "schema": {
            "type": "object",
            "required": ["name", "email", "reason"],
            "properties": {
              "name": { "type": "string" },
              "email": { "type": "string", "format": "email" },
              "system": { "type": "string" },
              "level": { "type": "string" },
              "duration": { "type": "string" },
              "reason": { "type": "string" }
            }
          }
        }
      }
    },
    "responses": {
      "200": {
        "description": "Request accepted; case created",
        "content": {
          "application/json": {
            "schema": {
              "type": "object",
              "properties": {
                "ok": { "type": "boolean" },
                "case_id": { "type": "integer" },
                "case_url": { "type": "string" },
                "slack_error": { "type": "string" }
              }
            }
          }
        }
      },
      "400": { "description": "Missing or invalid fields" },
      "502": { "description": "Failed to create the case" }
    }
  }
}
