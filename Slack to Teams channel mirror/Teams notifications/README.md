Webhook route `/teams-notifications` (`external_id` auth) for Microsoft Graph change notifications. Echoes `validationToken` for endpoint validation, drops notifications with an unexpected `clientState` or resource, and otherwise responds with `{messages: [{resource}]}` for [Mirror to Slack](<../Mirror to Slack/script.ts>). Emits an empty body when there is nothing to mirror, which stops the downstream step.
