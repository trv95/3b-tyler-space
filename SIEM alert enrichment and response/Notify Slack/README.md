Posts a notification message to the `#3b-notifications` Slack channel via [chat.postMessage](https://api.slack.com/methods/chat.postMessage).

Triggered manually (or link an upstream step to feed it a message). Uses the Slack connector for auth.
