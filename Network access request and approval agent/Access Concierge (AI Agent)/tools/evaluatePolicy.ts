import { tool } from "ai";
import { z } from "zod";
export default tool({description:"Evaluate a complete draft network access request against policy before confirmation or submission.",inputSchema:z.object({requester_type:z.enum(["employee","contractor"]),department:z.string().optional(),access_type:z.enum(["VLAN","VPN","guest Wi-Fi","specific segment"]),segment:z.string(),duration_days:z.number(),starts_at:z.string(),ends_at:z.string(),sponsor_email:z.string().optional()}),execute:async(input)=>{const base=process.env._3B_WORKFLOW_BASE_URL;if(!base)throw new Error("Workflow base URL unavailable");const r=await fetch(`${base}/network-access-api/evaluate`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"evaluate",...input}),signal:AbortSignal.timeout(10000)});if(!r.ok)throw new Error(`Policy service failed: ${r.status}`);return r.json();}});
